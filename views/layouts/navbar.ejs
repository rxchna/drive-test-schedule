<nav class="navbar navbar-expand-lg navbar-light bg-white py-3">
    <div class="container px-5">
        <a class="navbar-brand" href="/"><span class="fw-bolder text-primary">Book DriveTest</span></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0 fw-bolder">
                <li class="nav-item"><a class="nav-link" href="/">Dashboard</a></li>
                <% if(isLoggedIn && isTypeDriver) { %>
                    <li class="nav-item"><a class="nav-link" href="/g2_test">G2</a></li>
                    <li class="nav-item"><a class="nav-link" href="/g_test">G</a></li>
                <% } %>
                <% if(isLoggedIn && isTypeAdmin) { %>
                    <li class="nav-item"><a class="nav-link" href="/appointment">Appointment</a></li>
                <% } %>
                <% if(isLoggedIn && isTypeExaminer) { %>
                    <li class="nav-item"><a class="nav-link" href="/examiner">Examiner</a></li>
                <% } %>
                <% if(isLoggedIn) { %>
                    <li class="nav-item"><a class="nav-link" href="/auth/logout">Logout</a></li>
                <% } %>
                <% if(!isLoggedIn) { %>
                    <li class="nav-item"><a class="nav-link" href="/login">Login</a></li>
                    <li class="nav-item"><a class="nav-link" href="/register">Register</a></li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>